EX\_18APR - Word Frequency Counter

Write a program that:

&#x20;- Takes a string of text

&#x20;- Splits it into a list of words

&#x20;- Uses a for loop to count how many times each word appears (store results in a dictionary)

&#x20;- Uses a list comprehension to get all words that appear more than once

&#x20;- Prints those repeated words and their counts

