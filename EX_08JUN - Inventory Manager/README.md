EX\_08JUN - Inventory Manager

You have a store inventory. Each item is stored as a tuple: (item\_name, quantity, price).

&#x20;- Create a list of at least 5 items

&#x20;- Use a list comprehension to find all items that are low stock (quantity < 5)

&#x20;- Use enumerate() to print the full inventory numbered

&#x20;- Calculate and print the total inventory value (quantity × price for each item, summed up)

