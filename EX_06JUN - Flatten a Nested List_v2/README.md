EX\_06JUN - Flatten a Nested List\_v2

Write a function flatten(nested) that takes a list of lists (one level deep) and returns flat lists, one for odd and one for even numbers.

Rules:

&#x20;- Input is always a list of lists (no deeper nesting)

&#x20;- One list for odd numbers, and one list for even numbers

&#x20;- Preserve the original order

